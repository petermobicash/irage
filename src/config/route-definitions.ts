/**
 * Route Definitions
 * Extracted constants to avoid Fast refresh warnings
 * in routes.config.tsx
 */

// Export route constants to separate file to avoid Fast refresh warnings
export const routeMetaData = {
  publicRoutePaths: [
    // Route paths will be defined here to avoid Fast refresh warnings
  ],
  cmsRoutePath: '',
};